import React from 'react';
import { defineMessages } from 'react-intl';

export default defineMessages({
  'logo-text': {
    id: 'header.logoText',
    defaultMessage: 'Transitlinks',
    description: 'Transitlinks'
  }
});
